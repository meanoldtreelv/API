post:
  tags:
    - Transcription
  summary: Submit file for transcription
  description: Submit a file for transcription and recieve the unique submission ID for retreval. 
  operationId: echo
  security:
  responses:
    '200':
      description: OK
      headers:
      content:
        application/json:
          schema:
            type: string
          examples:
            response:
              value: Hello world!
        application/xml:
          schema:
            type: string
        text/csv:
          schema:
            type: string
    '400':
      description: Unauthorized
  requestBody:
    content:
      application/json:
        schema:
          type: string
          example: Hello world!
      application/xml:
        schema:
          type: string
          example: Hello world!
    description: Echo payload
    required: true
  x-codeSamples:
    - lang: C#
      source:
        $ref: ../code_samples/C_sharp/echo/post.cs
    - lang: PHP
      source:
        $ref: ../code_samples/PHP/echo/post.php
